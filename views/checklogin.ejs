<h1>Check login</h1>
<% if(r.uname === "test" && r.pwd === "12345") {%>
    <H2> SUCCESS</H2>
<%} else{ %>
    <font color="red"><H2> FAILED</H2></font>
    <b>TRY AGAIN!!</b>
<%} %>
<!-- <%= r.uname %>
<%= r.pwd %> -->